<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="mail_template_fleet_workshop_order_repaired" model="mail.template">
            <field name="name">Taller Flota - Unidad Reparada</field>
            <field name="model_id" ref="model_fleet_workshop_order"/>
            <field name="subject">Unidad {{ object.vehicle_id.license_plate }} Reparada - Orden de Trabajo {{ object.name }}</field>
            <field name="email_from">{{ user.email_formatted }}</field>
            <field name="email_to">{{ object.analyst_email }}</field>
            <field name="body_html"><![CDATA[
                <div style="margin: 0px; padding: 0px;">
                    <p>Estimado(a) {{ object.analyst_id.name }},</p>
                    <p>Le informamos que la unidad vehicular <strong>{{ object.vehicle_id.license_plate }}</strong> con número de chasis <strong>{{ object.vehicle_id.vin_sn }}</strong> ha sido reparada y está lista para su entrega.</p>
                    <p>Número de Orden de Trabajo: <strong>{{ object.name }}</strong></p>
                    <br/>
                    <p>Por favor, coordine la entrega con el conductor.</p>
                    <br/>
                    <p>Gracias,</p>
                    <p>{{ user.name }}</p>
                </div>
            ]]></field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>
